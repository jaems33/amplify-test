import React from 'react';

// const Dropdown: React.FunctionComponent<any> = (props: any) => {
  
//   return (
//     <select value={}>
//       {
//         props.product_sizes.map( (element: any) => <option key={element.id} value={element.id}>{element.name}</option>)
//       }
//     </select>
//   )
// }

/*
    <select value={scent} onChange={(event) => setScent(parseInt(event.target.value))}>
      <option key={NULL_ID} value={NULL_ID}>Select a Scent</option>
      {
        data.scents.map((element: any) => (<option key={element.id} value={element.id}>{element.name}</option>))
      }
    </select>
*/